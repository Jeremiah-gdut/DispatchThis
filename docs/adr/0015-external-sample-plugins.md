# 将样本语义移至独立外部插件

DispatchThis 作为 Binary Ninja 外部核心插件，只拥有工作流阶段、顺序及时序编排和与样本无关的功能型 API；每个具体样本由一个独立外部插件提供语义，新增样本意味着编写新插件，不提供样本家族或 bundled profile 支持。核心 API 应充分覆盖递归定义查找、PHI 和其他反复出现的 BNIL/数据流边界，而跳转目标公式、加解密算法及平坦化结构留给样本插件灵活实现；本决策取代 ADR-0002 和 ADR-0006。
