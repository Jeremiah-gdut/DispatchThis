# 以两层功能型 API 支撑样本语义

DispatchThis 的公开功能型 API 分为两层。基础原语层直接操作原生 Binary Ninja 对象，提供
变量身份、SSA 定义、PHI、表达式遍历、内存读取、CFG、支配关系和当前 IL 见证等精确能力；
完整证明层提供递归定义图、完整多值求解、调用目标切片、别名与地址逃逸、路径私有性和状态
写入证明等反复出现的深层分析。高层覆盖共性难点，低层允许样本表达特殊算法；两层都不得
静默返回不完整证据。核心不提供样本模式 DSL、通用规则引擎或具体样本识别器。
