# 提供者必须精确匹配核心 API 版本

每个外部样本插件都在 `SampleSemantics.api_version` 中写死一个整数，核心只注册与自身公开 API
版本精确匹配的提供者，并在不匹配时输出一次明确错误。破坏公开 Query、结果类型或功能型 API
时递增该整数；不提供版本范围、兼容适配器或旧接口 shim，因为它们会让尚未验证的样本插件
静默运行在不同契约上，而单个样本插件随破坏性版本更新才是更明确且更小的维护边界。
