# 用完整定义图取代隐藏深度上限

递归定义和 PHI API 不使用隐藏的固定 `max_depth`。核心按 Binary Ninja 变量与指令真实身份
去重，遍历全部到达定义和每个 PHI 输入，并在定义图中显式记录叶节点、边和循环。调用者只
能通过确定性的节点/边预算限制资源消耗；预算耗尽、任一必要定义缺失或任一 PHI 输入无法
读取时返回 `Inconclusive`，不能返回已访问的已知子集。循环检测独立于预算，不使用变量的
`str` 或 `repr` 作为身份。
