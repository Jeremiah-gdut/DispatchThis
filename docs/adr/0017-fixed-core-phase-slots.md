# 由核心定义固定恢复阶段槽位

DispatchThis 核心定义全部恢复阶段槽位及其名称、输入、锚点、依赖和失效规则；样本语义提供者只能实现或省略槽位，不得新增、插入或重排阶段。字符串恢复位于核心固定的 MLIL 槽位，但不以其他 DispatchThis phase 为前置；其余依赖按 pipeline 保持。真实新样本若需要新的生命周期位置，必须由 DispatchThis 核心正式增加并编排该槽位，而不能由样本插件修改 workflow DAG；本决策取代 ADR-0013。
